import styles from "../../styles/layout/components/Footer/footer.module.scss"

export default function Footer() {
    return (
        <footer className={`${styles['footer-full']}`}>
            <h1>Footer</h1>
            <p>Criado por Esparza</p>
        </footer>
    )
}